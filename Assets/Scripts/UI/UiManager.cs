
using UnityEngine;
using UnityEngine.UI;

public class UiManager : MonoBehaviour
{
    [SerializeField] Button pauseButton;
    [SerializeField] Dropdown playBackSpeedSelector;
    [SerializeField] Slider videoTimeLine;

    // subs to events which will be referenced in the manager etc. Should be pretty easy
}
